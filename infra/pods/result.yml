apiVersion: v1
kind: Pod
metadata:
  name: result
  labels:
    app: voteapp
    name: result
spec:
  containers:
    - name: result-app
      image: kodekloud/examplevotingapp_result:v1
      ports:
        - containerPort: 80
